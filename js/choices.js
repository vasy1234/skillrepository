window.addEventListener('DOMContentLoaded', function () {
  const element = document.querySelector(".select-1");
  const choices = new Choices(element, {
    searchEnabled: false,
    searchChoices: false,
  });
});
window.addEventListener('DOMContentLoaded', function () {
  const element = document.querySelector(".select-2");
  const choices = new Choices(element, {
    searchEnabled: false,
    searchChoices: false,
  });
});
window.addEventListener('DOMContentLoaded', function () {
  const element = document.querySelector(".select-3");
  const choices = new Choices(element, {
    searchEnabled: false,
    searchChoices: false,
  });
});
window.addEventListener('DOMContentLoaded', function () {
  const element = document.querySelector(".select-4");
  const choices = new Choices(element, {
    searchEnabled: false,
    searchChoices: false,
  });
});
window.addEventListener('DOMContentLoaded', function () {
  const element = document.querySelector(".select-5");
  const choices = new Choices(element, {
    searchEnabled: false,
    searchChoices: false,
  });
});
